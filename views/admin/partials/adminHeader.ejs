<!-- Sidebar -->

<ul class="navbar-nav bg-gradient-primary sidebar sidebar-dark accordion" id="accordionSidebar">

    <!-- Sidebar - Brand -->
    <a class="sidebar-brand d-flex align-items-center justify-content-center" href="/">
        <div class="sidebar-brand-icon rotate-n-15">
            <i class="fas fa-laugh-wink"></i>
        </div>
        <div class="sidebar-brand-text mx-3">Coffee <sup>ADMIN</sup></div>
    </a>

    <!-- Divider -->
    <hr class="sidebar-divider my-0">

    <!-- Nav Item - Dashboard -->
    <li class="nav-item menu-dashboard">
        <a class="nav-link" data-active="dashboard" href="/admin/dashboard">
            <i class="fas fa-fw fa-tachometer-alt"></i>
            <span key="dashboard">Dashboard</span></a>
    </li>

    <!-- Divider -->
    <hr class="sidebar-divider">
    <%if(menu.find(element => element.page == 'product')!=undefined && menu.find(element => element.page == 'product').view == '1'){%>
    <!-- Heading -->
    <div class="sidebar-heading menu-product" key="product">
        Product
    </div>

    <!-- Nav Item - Pages Collapse Menu -->
    <li class="nav-item menu-product">
        <a class="nav-link collapsed" href="#" data-toggle="collapse" data-target="#collapseTwo" aria-expanded="true"
            aria-controls="collapseTwo">
            <i class="fas fa-fw fa-list"></i>
            <span key="product">Product</span>
        </a>
        <div id="collapseTwo" class="collapse" aria-labelledby="headingTwo" data-parent="#accordionSidebar">
            <div class="bg-white py-2 collapse-inner rounded">
                <h6 class="collapse-header" key="product-management">Custom Components:</h6>
                <a class="collapse-item" key="product-list" data-active="productlist" href="/admin/productlist">Product
                    List</a>
                <%if(menu.find(element => element.page == 'product').edit == '1'){%>
                <a class="collapse-item" key="add-product" data-active="product" href="/admin/product">Add Product</a>
                <%}%>
                <a class="collapse-item" key="product-cat" data-active="category" href="/admin/category">Product
                    Categories</a>
                <a class="collapse-item" key="brand" data-active="brand" href="/admin/brand">Distributor</a>
            </div>
        </div>
    </li>
    <%}%>

    <!-- Nav Item - Utilities Collapse Menu -->
    <%if(menu.find(element => element.page == 'table-area')!=undefined && menu.find(element => element.page == 'table-area').view == '1'){%>
    <li class="nav-item menu-table-area">
        <a class="nav-link collapsed" href="#" data-toggle="collapse" data-target="#collapseUtilities"
            aria-expanded="true" aria-controls="collapseUtilities">
            <i class="fas fa-fw fa-list"></i>
            <span key="table">Coffee Table</span>
        </a>
        <div id="collapseUtilities" class="collapse" aria-labelledby="headingUtilities" data-parent="#accordionSidebar">
            <div class="bg-white py-2 collapse-inner rounded">
                <h6 class="collapse-header" key="table-management">Cofee Table Management</h6>
                <a class="collapse-item" key="table" data-active="table" href="/admin/table">Table</a>
                <a class="collapse-item" key="area" data-active="area" href="/admin/area">Area</a>
            </div>
        </div>
    </li>
    <%}%>

    <!-- Nav Item - Utilities Collapse Menu -->
    <%if(menu.find(element => element.page == 'order')!=undefined && menu.find(element => element.page == 'order').view == '1'){%>
    <li class="nav-item menu-order">
        <a class="nav-link collapsed" href="#" data-toggle="collapse" data-target="#collapseOrder" aria-expanded="true"
            aria-controls="collapseOrder">
            <i class="fas fa-fw fa-wallet"></i>
            <span key="order">Order</span>
        </a>
        <div id="collapseOrder" class="collapse" aria-labelledby="headingUtilities" data-parent="#accordionSidebar">
            <div class="bg-white py-2 collapse-inner rounded">
                <h6 class="collapse-header" key="order-management">Order Management</h6>
                <a class="collapse-item" key="order-current" data-active="order_current"
                    href="/admin/order/current">Current Order</a>
                <a class="collapse-item" key="order-today" data-active="order_today" href="/admin/order/today">Order
                    Today</a>
                <%if(menu.find(element => element.page == 'revenue')!=undefined && menu.find(element => element.page == 'revenue').view == '1'){%>
                <a class="collapse-item" key="revenue" data-active="revenue" href="/admin/revenue">Revenue</a>
                <%}%>
            </div>
        </div>
    </li>
    <%}%>

    <%if(menu.find(element => element.page == 'user')!=undefined && menu.find(element => element.page == 'user').view == '1'){%>
    <!-- Divider -->
    <hr class="sidebar-divider">

    <!-- Heading -->
    <div class="sidebar-heading menu-user" key="addons">
        Addons
    </div>

    <!-- Nav Item - Pages Collapse Menu -->
    <li class="nav-item menu-user">
        <a class="nav-link" href="#" data-toggle="collapse" data-target="#collapsePages" aria-expanded="true"
            aria-controls="collapsePages">
            <i class="fas fa-fw fa-user"></i>
            <span key="users">User</span>
        </a>
        <div id="collapsePages" class="collapse" aria-labelledby="headingPages" data-parent="#accordionSidebar">
            <div class="bg-white py-2 collapse-inner rounded">
                <h6 class="collapse-header" key="user-management">User Management</h6>
                <a class="collapse-item" key="user-list" data-active="users" href="/admin/users">User List</a>
                <%if(menu.find(element => element.page == 'user').edit == '1'){%>
                <a class="collapse-item" key="user-add" data-active="user_add" href="/admin/user/add">New User</a>
                <%}%>
                <%if(menu.find(element => element.page == 'role')!=undefined && menu.find(element => element.page == 'role').edit == '1'){%>
                <a class="collapse-item" key="role" data-active="role" href="/admin/role">Role</a>
                <%}%>
            </div>
        </div>
    </li>
    <%}%>

    <!-- Nav Item - Pages Collapse Menu -->
    <%if(menu.find(element => element.page == 'system')!=undefined && menu.find(element => element.page == 'system').edit == '1'){%>
    <li class="nav-item menu-system">
        <a class="nav-link" href="#" data-toggle="collapse" data-target="#collapseSetting" aria-expanded="true"
            aria-controls="collapseSetting">
            <i class="fas fa-fw fa-cog"></i>
            <span key="system">System</span>
        </a>
        <div id="collapseSetting" class="collapse" aria-labelledby="headingPages" data-parent="#accordionSidebar">
            <div class="bg-white py-2 collapse-inner rounded">
                <a class="collapse-item" key="system-setting" data-active="systemsetting" href="/admin/systemsetting">System
                    Setting</a>
            </div>
        </div>
    </li>
    <%}%>

    <!-- Divider -->
    <hr class="sidebar-divider d-none d-md-block">

    <!-- Sidebar Toggler (Sidebar) -->
    <div class="text-center d-none d-md-inline">
        <button class="rounded-circle border-0" id="sidebarToggle"></button>
    </div>

</ul>
<!-- End of Sidebar -->